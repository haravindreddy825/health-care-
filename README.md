# Healthcare Application — Clean Project Workspace

This workspace has been reset and prepared for a brand new healthcare application implementation while preserving the existing Supabase project connection.

## Preserved Supabase Configuration
* Supabase client configuration in `src/lib/supabase.js`
* Environment configuration in `.env` and `.env.example`
* Database migrations in `supabase/migrations/`

## Project Commands
* `npm install` — Install dependencies
* `npm run dev` — Start local development server
* `npm run build` — Build production bundle
